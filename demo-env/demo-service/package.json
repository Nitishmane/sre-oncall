{
  "name": "@sre-oncall/demo-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A small HTTP service with injectable faults, for demonstrating the healing pipeline.",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
